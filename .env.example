VITE_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Пароль админа для /admin (MVP: доступ через VITE-переменную в клиенте)
VITE_ADMIN_PASSWORD="change-me"

# Таймер экзамена (секунды)
VITE_EXAM_SECONDS="1800"

# Таймер пробной аттестации (секунды). Если не задан — берётся как 50% от VITE_EXAM_SECONDS.
VITE_TRIAL_SECONDS="900"

